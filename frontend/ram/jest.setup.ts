import "@testing-library/jest-dom";
//import "whatwg-fetch";

// async function readNextResponseJson(response: Response): Promise<any> {
//   const text = await response.text();
//   return JSON.parse(text);
// }
